import type { ReducedMotionMode } from '../scroll/reduce';

export const SCROLL_CONTAINER_DEFAULTS = {
  leftInset: 20,
  rightInset: 80,
  maskDuration: 0.3,
  reducedMotion: 'user' as ReducedMotionMode,
};
